{
  "name": "xiaohua.life",
  "version": "1.0.0",
  "description": "My blog http://www.xiaohua.life",
  "private": true,
  "dependencies": {
    "egg": "^2.15.1",
    "midway": "^1.0.0",
    "mysql2": "^1.7.0",
    "sequelize": "^4.44.4",
    "sequelize-typescript": "^0.6.11"
  },
  "devDependencies": {
    "midway-bin": "1",
    "midway-demo-lib": "1",
    "ts-node": "^8.10.2",
    "tslint": "^5.12.1",
    "typescript": "^3.9.7"
  },
  "engines": {
    "node": ">=8.9.0"
  },
  "scripts": {
    "start": "set NODE_ENV=local && midway-bin dev --ts",
    "debug": "set NODE_ENV=local && midway-bin debug --ts",
    "dev": "NODE_ENV=local && midway-bin dev --ts",
    "debugger": "NODE_ENV=local midway-bin debug --ts",
    "test": "midway-bin test --ts",
    "cov": "midway-bin cov --ts",
    "ci": "midway-bin cov --ts",
    "build": "midway-bin build -c",
    "lint": "tslint --fix -p tsconfig.json -t stylish"
  },
  "ci": {
    "version": "10"
  },
  "midway-bin-clean": [
    ".vscode/.tsbuildinfo",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "pengxiaohua",
  "license": "MIT"
}
